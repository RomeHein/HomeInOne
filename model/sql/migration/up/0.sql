-- Insert migration here in sql format
-- For each new migration, add a new file named 'n.sql', with 'n' being the highest migration number.

-- Always start your file with the following query:
SET search_path TO ${schema~};